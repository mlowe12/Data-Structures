#include <iostream> 
#include <vector>
#include <algorithm>
#include <memory>

class Solution
{
	public:
		bool searchMatrix(std::vector<std::vector<int> >& matrix, int target)
		{
			
		
			int N = 0; //where N is the width and height of the matrix.
			std::cin >> N; 
			for(int i = 0; i < N; ++i)
				for(int j = 0; j < N; ++j)
					std::cin >> matrix[i][j];
			return true;

			//access matrix again in O(1) time.
			int x; 
			int y; 
		
			matrix[x][y]; 
			
			for(int i = 0; i < N; ++i)
				for(int j = 0; j < N; ++j)
					
			return true; 
	}
			 
		

};